apiVersion: cluster.open-cluster-management.io/v1
kind: ManagedCluster
metadata:
  name: {{ .clusterName }}
  labels:
    cloud: OpenShift
    vendor: OpenShift
    name: {{ .clusterName }}
spec:
  hubAcceptsClient: true